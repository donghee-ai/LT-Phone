<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <meta-data android:name="com.samsung.android.vr.application.mode" android:value="dual" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/aw_launcher_round"
        android:icon="@mipmap/aw_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".OnboardingActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <!-- 프라이빗 모드 타일. BIND_QUICK_SETTINGS_TILE 은 시스템만 바인드할 수 있게 한다. -->
        <service
            android:name=".privacy.PrivateTileService"
            android:icon="@drawable/ic_private_tile"
            android:label="@string/private_tile_label"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".privacy.PrivateExpiryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="net.activitywatch.android.privacy.PRIVATE_EXPIRED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".AuthSettingsActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:exported="false"/>

        <!-- LT: 서버 push 설정 (시크릿을 소스에 넣지 않기 위한 화면) -->
        <activity
            android:name=".push.LTPushSettingsActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:exported="false"/>

        <activity
            android:name=".SyncSettingsActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:exported="false"/>

        <receiver
            android:name=".watcher.AlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="net.activitywatch.android.watcher.LOG_DATA" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name=".watcher.WebWatcher"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".BackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ActivityWatch runs a local HTTP server on-device to store and serve app usage data. All data stays on-device. The foreground service keeps the server continuously available so the ActivityWatch web UI remains accessible to the user. No predefined foreground service type (camera, microphone, location, health, media, etc.) covers this use case." />
        </service>

        <!-- LT: PushScheduler 의 AlarmManager 폴백. 서비스가 죽어도 전송을 이어간다. -->
        <receiver
            android:name=".push.PushAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="net.activitywatch.android.push.PUSH_ALARM" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SyncAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="net.activitywatch.android.SYNC_ALARM" />
            </intent-filter>
        </receiver>

        <service
            android:name=".watcher.MediaWatcher"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Category Time Widget -->
        <receiver
            android:name=".widget.CategoryTimeWidgetProvider"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="net.activitywatch.android.widget.ACTION_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/category_time_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
