# 설치 안내

LT Phone은 Google Play에 배포하지 않습니다. 공개 버전은 GitHub Releases의 APK로 설치합니다.

## APK 설치

1. 이 저장소의 **Releases**에서 최신 `lt-phone-<version>.apk`와 대응하는
   `.sha256` 파일을 내려받습니다.
2. 내려받은 APK의 SHA-256 값을 확인합니다.

   ```bash
   sha256sum -c lt-phone-<version>.apk.sha256
   ```

3. Android 설정에서 APK를 연 앱에 한해 "알 수 없는 앱 설치"를 허용하고 APK를 실행합니다.
4. 설치가 끝나면 해당 설치 허용을 다시 꺼도 됩니다.

릴리즈 페이지가 없거나 APK와 체크섬이 함께 제공되지 않은 버전은 설치하지 않는 것을 권장합니다.

## 권한

앱은 기능별로 다음 권한을 사용합니다.

- 사용 정보 접근: 실행 중인 앱과 사용 시간을 기록합니다.
- 접근성: 지원 브라우저의 현재 URL/제목을 읽는 기능에 사용합니다. 원하지 않으면 켜지 않아도 됩니다.
- 알림 접근: 재생 중인 미디어 제목과 앱 정보를 읽는 기능에 사용합니다. 선택 사항입니다.
- 알림: 백그라운드 기록 서비스가 실행 중임을 표시합니다.
- 인터넷: 로컬 웹 UI와 사용자가 설정한 자가 호스팅 서버에 연결합니다.

서버 전송 기능은 기본적으로 꺼져 있습니다. 서버 주소, 기기 이름, 공유 비밀값을 사용자가 직접
입력하고 기능을 켜기 전에는 외부 전송을 시도하지 않습니다.

## 업데이트 주의

Android는 같은 서명 키로 서명된 APK만 기존 앱 위에 업데이트할 수 있습니다. 공개 릴리즈용 키가
바뀌면 기존 앱을 제거한 뒤 다시 설치해야 하며, 앱 제거 시 로컬 기록도 삭제될 수 있습니다.
필요한 데이터는 제거 전에 앱의 내보내기 기능으로 별도 보관하세요.
